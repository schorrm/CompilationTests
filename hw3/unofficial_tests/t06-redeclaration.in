int foo(int x, int y) {
    return x + y;
}

int bar(byte x, int y) {
    return x + y;
}

void main () {
    int x = foo(1, 2);
    int y = bar(1 b, 2); // still okay
    int y = foo(1 b, 2); // not okay! redeclaration!
    return;
}